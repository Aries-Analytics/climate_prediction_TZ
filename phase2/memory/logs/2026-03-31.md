# Daily Log: 2026-03-31

> Session log for Tuesday, March 31, 2026

---

## Pipeline Run — March 31

**Status:** SUCCESS | Duration: 70s | Trigger: Scheduled (06:00 EAT)
**Forecasts:** 12 generated | Location: Morogoro | Data Quality: 100%
**Shadow Run:** 216 / 1,080 (20.0%) — 18 valid run-days
**Execution ID:** fff6f119-760f-494a-9daa-a1e6e99ef3b9

**Duration note:** 70s vs ~48s yesterday. ERA5 CDS API took 50s (accepted→running→successful) — external latency, not related to NDVI hook.

**NDVI hook issue diagnosed and fixed:**
- Scheduled run skipped NDVI Stage 3 — stale `.pyc` compiled from pre-hook code was running
- Deleted `/app/app/services/pipeline/__pycache__/orchestrator.cpython-311.pyc`
- Manually ran `collect_ndvi_observation()` for Mar 31 — inserted row but with wrong anomaly (+0.111) because baseline file was missing from container
- Root cause: `kilombero_ndvi_baseline.json` was written inside a container session during backfill and not persisted to host
- Fix: recomputed baseline in scheduler container, copied to host mount (`/opt/hewasense/app/phase2/backend/data/processed/`), corrected Mar 31 anomaly in DB to -0.0314
- Committed baseline file to repo with `git add -f` (data/ is gitignored; force-added this one file)
- From Apr 1 onwards: hook fires automatically with correct baseline

---

## Shadow Run Status

| Metric | Value |
|---|---|
| `forecast_logs` | 216 |
| Valid run-days | 18 (Mar 11, Mar 15–31) |
| Target | 1,080 |
| Progress | 20.0% |
| Next scheduled run | 2026-04-01 06:00 EAT |

**Note:** 18 total valid run-days. Current streak: 17 consecutive (Mar 15–31) + 1 isolated (Mar 11). 72 valid run-days remaining; revised end date Jun 12 — on track.

---

## Session Notes — March 31

- **Pitch deck v4 reviewed** — all changes from previous session correctly applied:
  - Slide 5 Box 2 inconsistency flagged: "6 types × 180 run-days" contradicts 90-day duration. Correct: "12 per day · 90-day run". Deck already sent so noted for v5.

- **Shadow run completion automation — fully implemented and deployed:**

  **Phase 1 — Orchestrator Stage 5 (completion detection):**
  - After Stage 4 (evaluation), counts `DISTINCT DATE(issued_at)` from `forecast_logs`
  - When `valid_run_days >= 90`: calls `generate_final_report()` + `send_shadow_run_complete_alert()`
  - Non-blocking — failure does not affect pipeline status

  **Phase 2 — `generate_final_report()` in `evidence_generator.py`:**
  - Computes Brier Score gate from `get_aggregate_metrics()`
  - Calls `compute_ndvi_proxy_basis_risk()` for proxy basis risk
  - Determines overall verdict: GO / NO-GO (Brier) / NO-GO (Basis Risk) / PENDING
  - Persists `shadow_run_final_report.json` to `data/processed/` (survives container restarts)
  - Served by `GET /api/v1/evidence-pack/final-report` (404 until day 90)

  **Phase 3 — `basis_risk_service.py` (new file):**
  - Joins `forecast_logs` × `ndvi_observations` by calendar month (year+month of issued_at vs observed_date)
  - Scope: evaluated primary-tier forecasts only (drought/crop_failure, ≥75%, ≤4mo lead, status=evaluated)
  - Corroboration: NDVI anomaly < -0.05 = vegetation stress agrees with model trigger
  - Flood forecasts excluded (NDVI not a reliable flood signal — noted in methodology)
  - Months with no NDVI observation excluded from denominator (gaps don't inflate %)
  - Returns: proxy_basis_risk_pct, gate_pass, corroborated/uncorroborated counts, per-month breakdown, methodology note
  - Served live by `GET /api/v1/evidence-pack/basis-risk` (returns 0 triggers until Jun 9 when first 3-month forecasts mature)

  **Phase 4 — `send_shadow_run_complete_alert()` in `alert_service.py`:**
  - Updated signature to accept proxy basis risk pct, gate pass, and detail dict
  - Slack message: Brier line + basis risk line (corroborated/total count) + verdict + next steps

  **Verified on server:**
  - `GET /api/v1/evidence-pack/basis-risk` → `{"proxy_basis_risk_pct": null, "total_primary": 0, ...}` (correct — no evaluated triggers yet)
  - `GET /api/v1/evidence-pack/final-report` → 404 with correct message (correct — shadow run not complete)

- **Key design decisions:**
  - Basis risk denominator excludes months with no NDVI data (fair — gaps must not inflate %)
  - Flood excluded by design (documented in methodology note surfaced in API response)
  - Result clearly labelled "indicative proxy" — harvest survey still needed for verified ground truth
  - `generate_final_report()` is idempotent — safe to re-run after day 90 (overwrites same file)

- **5–7 day payout timing confirmed** — conservative, defensible, consistent with industry benchmarks; "instant" would require proven payment API + pre-funded float + no human approval step — none of which exist yet

**Docs updated this session:** `PARAMETRIC_INSURANCE_FINAL.md` (shadow run completion + basis risk automation marked ✅)
