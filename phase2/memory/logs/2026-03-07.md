# Session Log — 2026-03-07

**Branch:** `phase2/feature-expansion`
**Commits:** `f8282f9` (ERA5 fix), `119e19e` (orchestrator Stage 3 + deploy.sh v2)

---

## Issues Diagnosed & Fixed

### 1. ERA5 400 Bad Request (commit `f8282f9`)
- **Symptom:** Pipeline partial failure — ERA5 returned 400 for March 2026 (current incomplete month)
- **Fix:** Cap `fetch_era5_data()` months to `range(1, last_complete_month+1)` AND cap `end_date` in `ingest_era5()`. Two places required.
- **File:** `backend/modules/ingestion/era5_ingestion.py`

### 2. ForecastLog Never Auto-Evaluated (commit `119e19e`)
- **Symptom:** All ForecastLogs stayed `pending` indefinitely — evaluation only reachable via manual API call
- **Fix:** Added Stage 3 to `execute_pipeline()` in `orchestrator.py` — calls `ForecastEvaluator.evaluate_pending_forecasts()` (non-fatal, try/except). Return key is `"processed"` not `"evaluated"`.
- **File:** `backend/app/services/pipeline/orchestrator.py`

### 3. Docker Compose v1 → v2 (commit `119e19e`)
- **Symptom:** `docker-compose` not found on server — server uses v2 plugin (`docker compose`)
- **Fix:** Replaced all `docker-compose` with `docker compose` in `scripts/deploy.sh` (6 occurrences)

### 4. Deployed to Server
- `git pull` + container restarts on `root@37.27.200.227`
- All 5 containers healthy: `climate_db_dev`, `climate_backend_dev`, `climate_frontend_dev`, `climate_pipeline_scheduler_dev`, `climate_pipeline_monitor_dev`

---

## Shadow Run Lifecycle

| Period | State |
|---|---|
| Mar 7 → Jun 5 (shadow run) | 90 × 12 = 1,080 ForecastLogs created, all `pending` (correct) |
| ~Jun 8 onward | 3-month forecasts mature, auto-evaluate via Stage 3 |
| Jul–Sep 2026 | 4/5/6-month horizons resolve on rolling basis |

---

## Learned Behaviors

1. `docker compose` (v2, plugin) only — `docker-compose` (v1) not on server
2. ERA5 month cap needed in TWO places — both fetch and ingest functions
3. `evaluate_pending_forecasts()` returns `{"processed": N}`, not `"evaluated"`
4. Volume-mounted backend (`./backend:/app`) — container restart, not rebuild, for Python changes
5. Advisory lock = session-level on dedicated connection — ORM commits don't release it
