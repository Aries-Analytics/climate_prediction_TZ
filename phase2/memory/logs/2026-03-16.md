# Daily Log: 2026-03-16

> Session log for Monday, March 16, 2026

---

## Events & Notes

### Public Landing Page — Built, Deployed, and Refined

**Scope:** `phase2/frontend/src/` — new `pages/LandingPage.tsx` + `components/landing/` (8 components)

**Stack addition:** Tailwind CSS v3 + shadcn/ui (coexists with MUI v5 via `corePlugins: { preflight: false }`)

**Components built:**
- `LandingNavbar.tsx` — fixed, scroll-aware, responsive
- `HeroSection.tsx` — full-viewport dark navy hero, constellation dots, CTAs
- `StatsBar.tsx` — 4 KPIs + Shadow Run Active amber badge
- `HowItWorksSection.tsx` — 4-step pipeline explanation
- `FeaturesSection.tsx` — 6 feature cards (one per dashboard module)
- `AboutSection.tsx` — mission + spec table
- `AccessCTASection.tsx` — shadow run framing + login CTA
- `LandingFooter.tsx` — brand, nav links, copyright

**Route change:** `/` now renders `<LandingPage />` instead of redirecting to `/dashboard/executive`.

**Deployed to:** `hewasense.majaribio.com` (Vite dev mode via Caddy reverse proxy, `docker-compose.dev.yml`)

**Docker fix required:** Anonymous `/app/node_modules` volume shadowed bind-mounted `./frontend:/app` — tailwindcss not visible inside container. Fixed by running `docker run --rm -v /opt/hewasense/app/phase2/frontend:/app -w /app node:18-alpine npm install` to install directly into the bind-mounted host directory, then force-recreate. GOTCHA guardrail "Docker Restart vs Force-Recreate Trap" confirmed.

---

### Copy Corrections (IP Protection + Accuracy)

**Parametric Triggers:** Removed fabricated "15–30% actuarial target bands" claim. Rewrote to describe calibration outcome, not thresholds.

**ML Forecasting:** Removed "gradient-boosted ensemble", "12-horizon", and "R² metrics" from public copy. Rewrote to describe probabilistic risk assessment outcomes only.

**Evidence Packs:** Removed fabricated "satellite imagery" reference. Verified actual ZIP contents against `evidence_generator.py` (metrics.json, logs_export.csv, model_compliance_statement.txt). Rewrote as a feature description.

**HowItWorks Step 02:** Removed model architecture exposure. Rewritten as outcome description.

**StatsBar:** Replaced "1,000+" with "1,000" (exact pilot size). Added "Shadow Run Active" amber badge. Replaced "12 Forecast Horizons" (potentially inaccurate) with "3 Perils Monitored". Changed "Climate Data / ERA5 + CHIRPS reanalysis" to "Historical Climate / ERA5 + CHIRPS · model training" for context.

**Hero subheadline:** Removed "aims to protect" (weak). Strengthened to state the product directly. Removed "ML-powered" from subheadline (redundant with "AI-Powered" headline — same claim twice, different terminology confuses audience).

**AccessCTA:** Reframed as shadow run context — explains what the product does in the pilot (real forecasts, trigger evaluation, evidence packs, no real payouts yet). Sets correct expectations for visitors who click "Access Dashboard".

**Footer:** Tagline changed "Tanzania Climate Parametric Insurance" → "Tanzania Climate Intelligence". Copyright year 2025 → 2026.

---

### Mobile Responsiveness Fixes

- Navbar height: `h-28` (fixed) → `h-16 sm:h-20 lg:h-28` (responsive)
- Logo height: `h-24` (fixed) → `h-12 sm:h-16 lg:h-24` (responsive)
- Hero padding: `pt-20` → `pt-20 sm:pt-24 lg:pt-32` (prevents content overlapping smaller mobile navbar)
- Pill badge: `text-xs sm:text-sm` + `whitespace-nowrap` (prevents wrapping on narrow screens)

---

## Commits This Session

| Hash | Message |
|------|---------|
| `eb218c3` | fix(landing): mobile-responsive navbar height and hero padding |
| `36aca1a` | fix(landing): update footer tagline to Tanzania Climate Intelligence |
| `7a99862` | fix(landing): correct copyright year to 2026 |
| `271dc0e` | fix(landing): sharpen hero subheadline and AccessCTA with product/shadow-run framing |
| `ee05e1c` | fix(landing): remove redundant ML-powered from hero subheadline |
| `dc60746` | fix(landing): centre footer logo correctly |
| `2524f09` | fix(landing): force footer logo to block-centre, handle asymmetric PNG padding |

All commits pushed to `origin/phase2/feature-expansion` and pulled to `root@37.27.200.227`.

---

## Archive Flag (Action Required)

Per Law #8 archive rule (>60 days old): the following `docs/current/` files are candidates for `docs/archive/phase3/`:
- `AUTOMATED_PIPELINE_STATUS_JAN2026.md`
- `TESTING_MONITORING_INFRASTRUCTURE_JAN2026.md`
- `JANUARY_2026_DASHBOARD_ENHANCEMENTS.md`
- `JANUARY_2026_PIPELINE_IMPROVEMENTS.md`

Confirm before moving — links in EXECUTIVE_SUMMARY reference some of these.
