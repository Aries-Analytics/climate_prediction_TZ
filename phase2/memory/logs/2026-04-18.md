# Daily Log: 2026-04-18

> Session log for Friday, April 18, 2026

---

## Session Notes

- **Merged `phase2/feature-expansion` → `main`** (373 commits). Used `git merge -s ours` to absorb main's unrelated history cleanly, then fast-forwarded main. All commit history preserved.
- **Updated deploy profiles** (`shadow-run.env`, `dev.env`) to `BRANCH="main"` — `live-pilot.env` was already on main.
- **Switched server (`37.27.200.227`) to main branch.** Confirmed checkout at `/opt/hewasense/app`, all 5 containers healthy, no restart needed. Code identical — only branch pointer changed.
- **Deleted `phase2/feature-expansion` branch** from remote and local. Single branch (`main`) going forward.
- **Updated root README** — replaced stale capstone-era README with concise HewaSense landing page (status, architecture, quick start, docs links, key technical details).
- **Removed legacy capstone files from repo root** — `EDA/`, `app/` (old Streamlit), `models/` (old RF/Ridge .pkl), `data/tanzania_weather_features_complete.csv`, corrupted root `requirements.txt`, root `.gitignore`. None referenced by current system. 62,164 lines removed, all preserved in git history.
- **Branching strategy adopted:** `main` = stable deployed code. Feature branches off main, merge back via PR. No long-lived development branches.

**Shadow run impact: none.** Running containers were not affected by any of these changes. Server was switched to track main after confirming code identity.

**Docs updated this session:** `README.md` (root), `phase2/scripts/deploy/profiles/shadow-run.env`, `phase2/scripts/deploy/profiles/dev.env`
