# Daily Log: 2026-04-07

> Session log for Tuesday, April 07, 2026

---

## Session Notes

### Pipeline Failure — Apr 7 Run (06:00 EAT)

Slack alert: era5 failed. chirps, nasa_power, ndvi, ocean_indices all succeeded — yesterday's tz-naive fix confirmed clean.

**Root cause identified and fixed:**

`era5_ingestion.py` line 408 used `hasattr(end_date, 'tzinfo')` to detect whether `end_date` needed timezone normalisation before comparing against `era5_safe_end` (a tz-aware `datetime`). However, plain `date` objects (returned by `incremental_manager.calculate_fetch_range()`) do not have a `tzinfo` attribute — `hasattr` returned `False`, the normalisation block was skipped entirely, and `date > datetime` raised `TypeError: can't compare datetime.datetime to datetime.date` at line 410.

This is a different bug from Apr 6 (same symptom family — type mismatch on date comparison — but different code path and different type pair: `date` vs `datetime`, not `tz-naive Timestamp` vs `tz-aware Timestamp`).

**Fix (commit `c77be7a`):**
- Replaced `hasattr(end_date, 'tzinfo')` guard with `isinstance(end_date, datetime)` check
- If plain `date`: promote with `datetime.combine(end_date, time.min).replace(tzinfo=timezone.utc)`
- If `datetime` but tz-naive: existing `.replace(tzinfo=timezone.utc)` path
- Added missing `time` to `from datetime import` line
- Deployed: git push → server pull → `docker restart climate_pipeline_scheduler_dev`
- Scheduler healthy, next run: `2026-04-08 06:00:00+03:00`

**All four sources (chirps, nasa_power, ndvi, era5) should succeed tomorrow.**

**Docs updated this session:** none (pipeline bug fix only)

---

## Session Notes — April 7 (continuation)

- **Docs folder Phase A restructure complete:** Deleted empty placeholder files; created `docs/validation/`, `docs/pilots/kilombero/`, `docs/archive/legacy-reports/`; moved `KILOMBERO_BASIN_PILOT_SPECIFICATION.md` → `docs/pilots/kilombero/`; created `docs/archive/_INDEX.md` documenting Phase A moves and full Phase B plan; updated `docs/README.md` to v3.5
- **Cleared docs/ root of stray .md files:** 8 loose `.md` files moved to correct subdirectories (references/, current/, etc.)
- **Cleared phase2/ root of stray scripts:** `train_pipeline.py`, `run_evaluation.py`, `audit.py` → `scripts/`; `verify_data_leakage.py` → `scripts/verification/`
- **Fixed broken path references after script moves (commit `73721e2`):** All 4 scripts used `Path(__file__).resolve().parent` which pointed to `phase2/` when at root but now resolves to `scripts/` or `scripts/verification/`. Fixed: `scripts/*.py` → `.parent.parent`; `scripts/verification/*.py` → `.parent.parent.parent`. Both occurrences in `audit.py` fixed (config path line 25 + `phase2_dir` line 104).
- **Decision:** Phase B docs restructure deferred to post-June gate (shadow run completion). Rationale: Phase A was low-risk moves; Phase B involves content merges that require validation context.

**Docs updated this session:** `docs/README.md` (v3.5), `docs/archive/_INDEX.md` (new)

---

## Session Notes — April 7 (second continuation)

- **External memory drift root cause identified and fixed:** Context overflow mid-session causes summary to describe intent as confirmed; external memory has no git history so drift goes undetected. Two-part fix: (1) mandatory verification step added to `/log-session` Step 6b — read external memory before writing, compare against repo, backfill gaps; (2) explanatory note added on why the file can drift silently; checklist item added.
- **`docs/README.md` comprehensive update (commit `986294b`):** Replaced stale Jan 2026 What's New entries with Mar–Apr 2026 milestones; fixed Project Status (v3.4→v3.5, Last Updated Mar 8→Apr 7); split "Pilot Locations: 6" into Production Pilot (Morogoro) vs Training Locations (6); fixed two broken paths in Additional Resources.
- **`phase2/README.md` comprehensive update (commit `f828ed5`):** Rewrote Project Structure tree (added backend/, frontend/, args/, configs/, tools/, memory/; removed duplicate tests/utils blocks; corrected scripts/ contents); fixed all script paths (`train_pipeline.py`→`scripts/train_pipeline.py`); corrected feature count 77→83; test count 45+→180+ consistently; ERA5 env var `ERA5_API_KEY`→`ECMWF_DATASTORES_KEY`; March 2026 note "3/8 events"→100% catch rate on 2 confirmed disasters; updated Documentation section to current subdirectory paths; fixed Quick Start and Troubleshooting script refs.

**Docs updated this session:** `.claude/commands/log-session.md`, `docs/README.md`, `README.md`

---

## Session Notes — April 7 (third continuation)

- **Comprehensive `docs/references/` audit (commit `eeacf51`):** Checked all 22 .md files for stale data. Fixed in 7 files:
  - `PARAMETRIC_INSURANCE_FINAL.md` — 9 fixes: 100-farmer→1,000-farmer, $1,988 annual premium→$20,000, heat_stress×4→crop_failure×4 (heat_stress excluded: rainfall model ≠ temperature model), hardcoded Day 21/252 run counts removed (→Evidence Pack dashboard pointer), Jun 12→Jun 2026, formal TIRA submission date hedged (mid-2026, Go/No-Go contingent), broken links fixed (`INSURANCE_TRIGGER_RECALIBRATION_SUMMARY.md` removed, `6_LOCATION_EXPANSION_SUMMARY` path corrected)
  - `ML_MODEL_REFERENCE.md` — header version v3.2/Mar 8 → v3.6/Mar 21 (footer was already correct)
  - `BUSINESS_CASE_AND_DEPLOYMENT_RATIONALE.md` — hardcoded run counts removed, Jun 12→Jun 2026
  - `PROJECT_OVERVIEW_CONSOLIDATED.md` — "persistent job store"→"in-memory job store (prevents phantom runs from stale next_run_times)", Jun 12→Jun 2026 (×2), broken `PARAMETRIC_INSURANCE_REFERENCE.md` link fixed, stale near-term roadmap updated
  - `PAYOUT_CALCULATION_MODEL.md` — Jun 12→Jun 2026; `KILOMBERO_BASIN_PILOT_SPECIFICATION.md` source path `docs/references/`→`docs/pilots/kilombero/` (×2)
  - `FRONTEND_DASHBOARDS_COMPLETE_REFERENCE.md` — "Jun 12, 2026"→"mid-2026"
  - `PHASE_3A_IMPLEMENTATION_SUMMARY.md` — stale `docs/references/KILOMBERO_BASIN_PILOT_SPECIFICATION.md` path (×2, replace_all)
- **Key antipatterns confirmed and removed:** (1) hardcoded "Day N of 90 / M/1,080 forecasts / X.X%" counts in reference docs — these go stale immediately; correct practice is pointer to live Evidence Pack dashboard; (2) specific date "Jun 12" in external/reference docs — replaced with "Jun 2026" per timeline tone protocol; (3) "persistent job store" claim — system uses in-memory intentionally
- **No affected high-level docs needed updating** (EXECUTIVE_SUMMARY, HEWASENSE_EXTERNAL_BRIEF, KILOMBERO_BASIN_PILOT_SPECIFICATION already correct — only references/ subfolder had stale data)

**Docs updated this session:** 7 files in `docs/references/` (see above)
