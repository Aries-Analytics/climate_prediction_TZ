# Session Log — 2026-03-08

**Branch:** `phase2/feature-expansion`
**Commits:** `e8f2231` (scheduler TZ fix), `95ac287` (model metrics seed), `4ee4275` (climate data seed), `edf70bf` (DATA_PIPELINE_REFERENCE UTC fix), `7149d08` (docker-compose v1 sweep), `d914daf` (shadow run status docs), `29cbbee` (agent triggers expansion)

---

## Issues Diagnosed & Fixed

### 1. Scheduler Timezone Bug — CRITICAL (commit `e8f2231`)
- **Symptom:** No Slack alert at 6AM EAT on shadow run day 1 (Mar 8). Alert came at 9AM EAT.
- **Root cause:** `CronTrigger.from_crontab(self.schedule)` defaults to UTC. `BackgroundScheduler(timezone=...)` does NOT propagate timezone to CronTrigger — must be passed explicitly.
- **Fix:** `CronTrigger.from_crontab(self.schedule, timezone=self.timezone)` — one argument added
- **File:** `backend/app/services/pipeline/scheduler.py:215`
- **Proof:** Log before fix: `Next scheduled run: 2026-03-08 06:00:00+00:00`. After: `2026-03-09 06:00:00+03:00`
- **Impact:** Shadow run day 1 (Mar 8) MISSED. First correct run = Mar 9, 6AM EAT.

### 2. Model Performance Dashboard Empty (commit `95ac287`)
- **Root cause:** `model_metrics` table empty after container rebuild — only users were re-seeded
- **Fix:** Updated `scripts/seed_models_and_forecasts.py` with actual test-set metrics from `outputs/models/latest_training_results.json` (training date 2026-03-05):
  - XGBoost: r²=0.867, rmse=0.401; Ensemble: r²=0.840, rmse=0.439; LSTM: r²=0.787, rmse=0.510; RF: r²=0.781, rmse=0.513

### 3. Climate Insights Dashboard Empty (commit `4ee4275`)
- **Root cause:** `load_climate_data.py` hardcoded `TANZANIA_LAT/LON` for all rows → all 6 locations per date collapsed to 1 via upsert (313 rows stored, not 1,878)
- **Also:** Correct dataset is `phase2/data/processed/master_dataset.csv` (real unscaled values), NOT `phase2/outputs/processed/master_dataset.csv` (z-score normalized — wrong)
- **Fix:** Per-row `row_lat = float(row.get('latitude', ...))` / `row_lon` extraction + SCP'd real dataset to server
- **Result:** 1,878 records loaded, all 5 chart sections populated (2000–2026)

### 4. SSH Automation (no commit — config only)
- **Problem:** Every SSH call from Claude Code subprocess prompted for passphrase
- **What failed:** bashrc SSH agent, Windows OpenSSH named pipe, ControlMaster (Unix sockets unsupported on Windows)
- **Solution:** Passphraseless key `~/.ssh/id_hewasense_auto` + `~/.ssh/config` with `IdentityFile ~/.ssh/id_hewasense_auto`
- All subsequent SSH/SCP calls instant and silent

---

## Doc Sweep (Global — GOTCHA Law #8)

Searched for stale UTC timezone refs across `docs/current/`, `docs/references/`, `goals/`, `context/`:
- `docs/AUTOMATED_PIPELINE_DEPLOYMENT.md` — `"0 3 * * *" + TIMEZONE=UTC` → `"0 6 * * *" + Africa/Dar_es_Salaam`
- `docs/references/DATA_PIPELINE_REFERENCE.md` — `Daily at 06:00 UTC`, `PIPELINE_TIMEZONE=UTC` → EAT

**Staleness class missed (found later):** Status transitions. Three docs still said "forward validation pending":
- `docs/current/EXECUTIVE_SUMMARY.md` → fixed (shadow run ACTIVE)
- `docs/references/PROJECT_OVERVIEW_CONSOLIDATED.md` → fixed
- `docs/Kilombero Pilot/KILOMBERO_BASIN_PILOT_SPECIFICATION.md` → fixed

**Lesson:** Global doc sweep must check ALL 4 staleness classes: metrics, status transitions, stale dates, deprecated commands.

---

## Dashboard State

| Dashboard | State |
|---|---|
| Executive | Working — simulated display data |
| Climate Insights | ✅ Populated — 1,878 rows, 6 locations, 2000–2026 |
| Model Performance | ✅ Populated — 4 models with real training metrics |
| Evidence Pack | Empty (correct) — fills as shadow run progresses |
| Early Warning | Empty (correct) — fires on threshold breaches only |
| Risk Management | Empty (correct) — needs forecast resolution (Jun+) |

---

## Shadow Run Status

- **Day 1 (Mar 8):** MISSED — scheduler fired at 9AM EAT due to timezone bug
- **Day 2 (Mar 9):** First correct run at 6AM EAT — watch Slack

---

## Learned Behaviors

1. `CronTrigger.from_crontab()` timezone NOT inherited from `BackgroundScheduler`. Must pass explicitly.
2. Two `master_dataset.csv` files — `data/processed/` (real values) vs `outputs/processed/` (z-score normalized, NOT for display)
3. Global doc sweep must cover 4 staleness classes: metrics, status transitions, stale dates, deprecated commands
4. SSH on Windows Claude Code subprocess: only passphraseless key + `~/.ssh/config` works
5. In-repo GOTCHA memory (`phase2/memory/`) and Claude Code project memory (`.claude/projects/.../memory/`) are two separate systems — keep both synced

---

## Second Session — Frontend & API Bug Fixes

**Additional commits:** Vite config fix, ExecutiveDashboard useMemo refactor, simulation API rename, `c82382d` (validation text dynamic values)

---

### Issues Diagnosed & Fixed

#### 5. 502 Bad Gateway — Frontend Down (vite.config.ts)
- **Symptom:** `GET /node_modules/.vite/deps/react_jsx-dev-runtime.js 502` — all dashboard routes returning gateway errors.
- **Root cause:** Previous session added `optimizeDeps.include: ['plotly.js', 'react-plotly.js']`. Vite's esbuild pre-bundler cannot resolve Node.js-specific `require('buffer/')` inside plotly → entire `.vite/deps` cache poisoned, breaking ALL modules including React itself.
- **Intermediate failure:** Added `optimizeDeps.exclude` → new error: `react-plotly.js does not provide an export named 'default'` — Vite served raw CJS without ESM conversion.
- **Fix:** Removed entire `optimizeDeps` block from `vite.config.ts`. Also cleared stale cache on server: `docker exec climate_frontend_dev rm -rf /app/node_modules/.vite`.
- **Rule:** `optimizeDeps.include` → esbuild (fails on Node.js CJS); `optimizeDeps.exclude` → raw CJS to browser (fails ESM). Vite default auto-detection is the only working option for react-plotly.js. `manualChunks` in `build.rollupOptions` is safe (Rollup-only, production builds).

#### 6. ExecutiveDashboard — 800ms Fake Loading Spinner
- **Root cause:** `useState(true) + setTimeout(800)` simulated async load for locally-computed `generateMockData()` — always synchronous, never an actual API call.
- **Fix:** `useMemo(() => generateMockData(selectedYear), [selectedYear])` — zero-delay. Real alerts fetch in independent `useEffect` on mount (parallel, non-cascading).
- **Rule:** Mock/local-computed data must never use async loading patterns. `useMemo` for derived state, `useEffect` for network calls.

#### 7. Historical Validation Tab — 500 Error (simulation.py:143)
- **Symptom:** Risk Management → Historical Validation tab returned 500: `"BacktestingService object has no attribute 'run_simulation'"`.
- **Fix:** `service.run_simulation(simulation_id)` → `service.run_phase_based_simulation(simulation_id)`. One-line fix.
- **Seeded:** Two simulations — ID 1 (out-of-sample 2000-2014, location_id=6) and ID 2 (in-sample 2015-2025, location_id=6). Dashboard fetches highest completed ID → ID 2.

#### 8. BacktestingValidation — Hardcoded Text Mismatches (commit `c82382d`)
- **Symptom 1:** Banner showed "backtested against 10 years" — simulation covers 2015–2025 = 11 years.
- **Symptom 2:** Chart title hardcoded "(2015-2025)" — won't adapt to different simulations.
- **Fix:** `"10 years"` → `{totalYears} years`; `"(2015-2025)"` → `({report.executive_summary.period})`.

---

### Dashboard State (Updated After Second Session)

| Dashboard | State |
|---|---|
| Executive | ✅ Working — instant render via `useMemo`, real alerts merged on load |
| Climate Insights | ✅ Populated — 1,878 rows, 6 locations, 2000–2026 |
| Model Performance | ✅ Populated — 4 models with real training metrics |
| Evidence Pack | Empty (correct) — fills as shadow run progresses |
| Early Warning | Empty (correct) — no threshold breaches yet (confirmed expected state) |
| Risk Management → Historical Validation | ✅ RESTORED — 11-year backtest (2015-2025), 3/8 validated events, dynamic text |
| Risk Management → other tabs | Pending forecast resolution (~Jun 2026) |

---

### Second Session Learned Behaviors

6. **Vite + react-plotly.js:** Never use `optimizeDeps.include/exclude` for plotly. Default auto-detection only. `manualChunks` (Rollup) is safe for production.
7. **BacktestingService method:** API calls `run_phase_based_simulation()`, NOT `run_simulation()`.
8. **Backtesting independence from ML:** Phase-based trigger engine (drought/flood vs CHIRPS/ERA5) is separate pipeline from XGBoost. Data leakage fix does NOT affect Historical Validation results.
9. **In-sample vs out-of-sample for dashboard:** KNOWN_EVENTS (2016–2023) are outside out-of-sample range (2000–2014) → 0/8 validated for ID 1. In-sample (2015–2025) → 3/8 validated. Dashboard shows highest completed simulation ID.
10. **3/8 vs 4/8 explained:** 2019 mismatch — model detected drought; OCHA documented flood. Different events, same year. Not a model error.

---

## Third Session — Gitignore Fix + Medium Article Finalisation

**Commits:** `c5b1714` (gitignore + memory tracked), article rename (DRAFT→FINAL, pending push)

---

### Issues Resolved

#### 9. In-repo GOTCHA Memory Gitignored (commit `c5b1714`)
- **Problem:** `memory/` was in `.gitignore` under "Session Artifacts" (commit `a56932d`). This meant GOTCHA memory was machine-local and didn't travel with the repo.
- **Fix 1:** Removed `memory/` from `phase2/.gitignore` Session Artifacts block.
- **Fix 2:** Changed `logs/` → `/logs/` in `phase2/.gitignore` (anchors to root, prevents matching `memory/logs/`).
- **Fix 3:** Root `.gitignore` also had global `logs/` rule — added negation patterns `!memory/`, `!memory/logs/`, `!memory/logs/**` to `phase2/.gitignore`.
- **Result:** All 6 memory files (`MEMORY.md` + 5 session logs) now tracked and pushed to origin.
- **Verified:** `git ls-files memory/` confirms all 6 tracked. `git check-ignore -v memory/MEMORY.md` returns exit 1 (no rule matches).

#### 10. VS Code Stale Gitignore Decoration
- **Symptom:** User saw memory files showing as gitignored in VS Code even after they were committed and tracked.
- **Root cause:** VS Code gitignore decoration cache is stale — does NOT reflect live git state.
- **Fix:** `git ls-files memory/` is the authoritative check. VS Code refresh resolves the UI.

#### 11. Medium Article Part 2 — Finalised
- **Process:** Two drafts existed — old draft (specific, MLOps-narrative, "Valley of Death") and new draft (public-friendly, balanced disclosure). Merged: new draft as base, injected critical specifics from old draft only.
- **Editorial fixes applied:** Part 1 link placeholder, "we"→"I" throughout, parametric insurance explanation, "four horizons"→"3 to 6 months ahead", pipeline diagram placeholder, non-repetitive conclusion with forward-validation punchline.
- **Key judgment:** Tech stack section retained (signals credibility to technical readers). Empty dashboard screenshot rejected in favour of pipeline diagram placeholder.
- **Renamed:** `MEDIUM_ARTICLE_PART_2_DRAFT.md` → `MEDIUM_ARTICLE_PART_2_FINAL.md`
- **Pending before publish:** Insert Part 1 URL, create and insert pipeline diagram, add cover image.

---

### Third Session Learned Behaviors

11. **VS Code gitignore decoration is unreliable.** Always verify with `git ls-files` or `git check-ignore -v`. The IDE cache can show tracked files as ignored.
12. **Negation patterns in subdirectory .gitignore override parent rules.** `!memory/logs/` in `phase2/.gitignore` successfully overrides `logs/` in root `.gitignore`.
13. **`state.json` is correctly gitignored** — Claude Code session state, machine-specific, should never travel with repo.
14. **Medium article balance rule:** Use public draft as base (controls disclosure level). Inject specifics from internal draft only where they add credibility without revealing architecture (concrete numbers: R², feature counts; operational facts: 6AM EAT, 12 forecasts).
15. **Medium article pre-publish checklist:** (1) link to previous part, (2) consistent pronoun (I vs we), (3) explain domain terms (parametric insurance), (4) concrete time horizons, (5) non-repetitive conclusion, (6) pipeline diagram instead of empty dashboard screenshot, (7) cover image.
